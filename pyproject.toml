[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "majesty-gold-hd-mod-manager"
version = "0.3.8"
description = "Organize, combine, and launch Majesty Gold HD mods and quests."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "Majesty Gold HD Mod Manager contributors" }
]
dependencies = []

[project.optional-dependencies]
dev = []
gui = [
  "PySide6-Essentials>=6.7,<6.9"
]
package = [
  "pyinstaller>=6.10,<7"
]

[project.scripts]
majesty-cam = "majesty_cam.cli:main"

[project.gui-scripts]
majesty-mod-manager = "majesty_cam.manager.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/majesty_cam"]
